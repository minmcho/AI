fastapi==0.111.0
uvicorn[standard]==0.29.0
asyncpg==0.29.0
httpx==0.27.0
pydantic==2.7.0
python-dotenv==1.0.1

# ── AI / Claude ───────────────────────────────────────────────
anthropic==0.34.0          # Claude API + prompt caching + extended thinking

# ── ML / Vector / Clustering ──────────────────────────────────
numpy==1.26.4
scikit-learn==1.4.2
hdbscan==0.8.38.post1      # density-based clustering (preferred)
pgvector==0.3.2            # asyncpg pgvector extension support

# ── Image processing ──────────────────────────────────────────
Pillow==10.3.0
